# Environment Configuration for WaveGuard API

# Development
ENVIRONMENT=development
HOST=127.0.0.1
PORT=8000

# Production (uncomment for production deployment)
# ENVIRONMENT=production
# HOST=0.0.0.0
# PORT=8000

# Security (add for production)
# SECRET_KEY=your-secret-key-here
# ALLOWED_HOSTS=yourdomain.com,your-app.vercel.app

# Database (if you add database functionality later)
# DATABASE_URL=postgresql://user:password@localhost/waveguard

# Logging
LOG_LEVEL=INFO

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,https://yourdomain.com
